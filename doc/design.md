# YetAnotherVideoEditor (YAVE) 詳細設計書

C++17 / Qt 6 / FFmpeg ベースの、クロスプラットフォーム(Windows 主 / macOS 移植性確保)
ハイエンド動画編集アプリケーションの詳細設計書。

- **バージョン**: 1.0
- **最終更新**: 2026-08-24
- **対象読者**: 本アプリケーションの実装担当者
- **前提**: 本書のコードはそのまま実装の出発点として使える粒度で記述している

---

## 目次

| 章 | ファイル | 内容 |
|---|---|---|
| 1 | [全体アーキテクチャ設計](design/01-architecture.md) | コンポーネント図・スレッドモデル・CMake ターゲット切り分け |
| 2 | [ディレクトリ構成](design/02-directory-layout.md) | フォルダ構造・モジュール分割・依存調達 |
| 3 | [タイムライン & レンダリングエンジン](design/03-timeline-render.md) | 無限レイヤー構造・4K60p 合成パイプライン |
| 4 | [ビデオエンジン (FFmpeg + GPU)](design/04-video-engine.md) | HW デコード・ゼロコピー・エンコード |
| 5 | [オーディオエンジン](design/05-audio-engine.md) | オーディオファースト同期・PDC・ロックフリー |
| 6 | [字幕エンジン](design/06-subtitle-engine.md) | SRT → 字幕区間・グリフ単位アニメーション・エフェクトプラグイン |
| 7 | [マルチモーダル生成AIエンジン](design/07-ai-orchestrator.md) | T2V/I2V/V2V/TTS/STT の非同期生成とコミット |
| 8 | [プラグインホスト](design/08-plugin-host.md) | VST3 / AviUtl / 字幕エフェクト・ネイティブハンドル |
| 9 | [プロジェクト保存 (JSON)](design/09-project-io.md) | シリアライズ・スキーマバージョニング |
| 10 | [国際化 (日英切替)](design/10-i18n.md) | QTranslator ランタイム切替・プラグイン文字列 |
| 11 | [クラスリファレンス](design/11-class-reference.md) | 主要クラスの .h 完全定義 |
| 12 | [重要実装コード断片](design/12-snippets.md) | CMake / IPlugView / JSON シリアライズ ほか |
| 13 | [AIトラック (演出指示 / 絵コンテ)](design/13-ai-track.md) | カット単位の演出指示・出力トラックの自動生成・バッチ生成・アニマティック |

---

## 1. プロダクト概要

YAVE は以下を同時に満たすことを目標とする。

1. **トラック数無制限**のノンリニア編集。ビデオ / オーディオ / 字幕 / AI生成 / 絵コンテ の
   各トラックを動的に追加・削除・並び替えできる。
2. **4K (3840x2160) / 60fps のリアルタイムプレビュー**。複数レイヤーの GPU 合成を含む。
3. **FFmpeg C API 直叩き**による自作メディアエンジン。OS 固有 GPU 支援を第一級で使う。
4. **マルチモーダル生成AI** をタイムラインの区間指定で呼び出せる。
   さらに **AIトラック**の各区間にカット単位の演出指示を置き、
   そこから必要なトラック (映像 / ナレーション / BGM / SE / 字幕) を
   自動生成できる ([13章](design/13-ai-track.md))。
5. **VST3 / AviUtl プラグイン**をホストできる。
6. プロジェクトは**人間可読な JSON** で完全永続化される。
7. UI は**日本語 / 英語をランタイム切替**できる。
8. UI のパネル (メディアライブラリ / エフェクトライブラリ / ファイルブラウザ /
   コンソール / インスペクタ / タイムライン / AI タスク一覧 / 絵コンテボード) は
   **ドッキング位置とタブ構成をユーザーが設定**できる。素材と効果は
   **エクスプローラ風のフォルダ UI** で整理し、タイムラインへドラッグして使う
   ([1.7](design/01-architecture.md#17-ui-パネル構成-ドッキング--タブ))。

## 2. 技術スタック

| 領域 | 採用技術 | 備考 |
|---|---|---|
| 言語 | C++17 | `std::optional` / `std::variant` / 構造化束縛 を活用 |
| UI | Qt 6.6+ (QML + Widgets ハイブリッド) | メインUIは QML、プラグイン窓は QWidget |
| ビルド | CMake 3.24+ | `qt_add_executable` / `qt_add_qml_module` / `qt_add_translations` |
| 描画 | Qt RHI (`QRhi`) | Win: D3D11 (既定) / D3D12、Mac: Metal |
| メディア | FFmpeg 6.x (libav*) | C API を直接使用 |
| HWデコード | D3D11VA / NVDEC(CUDA) / QSV / VideoToolbox | |
| HWエンコード | NVENC / QSV / VideoToolbox | |
| オーディオIO | Win: WASAPI (排他/共有)、Mac: CoreAudio | `QAudioSink` は低レイテンシ要件を満たさないため自前実装 |
| AI推論 | ONNX Runtime 1.17+ (C++ API) | EP: DirectML / CUDA / CoreML |
| AI (リモート) | HTTP/JSON (`QNetworkAccessManager`) | ComfyUI / OpenAI互換 / Replicate 等 |
| プラグイン | VST3 SDK 3.7+ / AviUtl(x64) / 自前字幕エフェクトABI | |
| 永続化 | `QJsonDocument` | |

## 3. 設計上の全体方針(全章共通の前提)

本書の各章はこれらを前提として記述している。個別章で再説明しない。

### 3.1 時間表現は有理数で統一する
すべてのタイムライン時刻は `Rational`(分子/分母の整数対)と、それを基準とした
`int64_t` のフレーム番号で保持する。`double` 秒での保持は禁止する。

> **理由**: 60000/1001 (59.94fps) や 24000/1001 (23.976fps) といったドロップフレーム系タイムベースを
> 扱う前提で、`double` 秒での往復変換は編集点のズレとして必ず顕在化する。特に
> 「クリップを分割して再結合したら 1 フレームずれる」「SRT を取り込んだら境界が 1 フレーム手前になる」
> といった不具合はユーザーの信頼を致命的に損なう。

プロジェクトは単一の `timebase` を持つ(既定 `60000/1001`)。素材側のタイムベースは
取り込み時にプロジェクトタイムベースへ変換する。

> **外部境界での例外**: SRT 取り込み ([6章](design/06-subtitle-engine.md)) と
> LLM が返すカット尺 ([13.7](design/13-ai-track.md)) は、仕様上どうしても秒で入ってくる。
> これらは**パーサの境界で `secondsToFrames(..., RoundMode)` によりフレームへ変換し、
> 秒のまま保持しない**。「保持を禁じる」のであって「受け取りを禁じる」のではない。

### 3.2 編集操作はすべて Undo コマンドである
タイムラインを変更するあらゆる操作は `QUndoCommand` 派生として実装し、`QUndoStack` に積む。
これは UI 操作に限らず、**AI 生成結果のコミット**や **SRT 一括取り込み**にも適用する。

> **理由**: 「AI が生成したクリップの挿入だけ Undo できない」という状態は実用上破綻する。
> 生成には時間がかかるため、ユーザーは必ず「試して、気に入らなければ戻す」使い方をする。

### 3.3 ID は UUID
`Clip` / `Track` / `AiGenerationTask` / プラグインインスタンス はすべて `QUuid` で識別する。
配列インデックスを永続 ID として使わない(並び替えで壊れるため)。

### 3.4 アセットパスはプロジェクト相対
プロジェクトファイル (`*.yave`) からの相対パスで保存する。絶対パスは持たない。

> **理由**: Windows と macOS の間でプロジェクトを持ち運ぶ要件があるため。
> ドライブレターや `/Users/...` を保存に含めた時点で可搬性が失われる。

相対化できない場所(別ドライブ等)を参照した場合はプロジェクト保存時に警告し、
「アセットをプロジェクトフォルダへ収集する」操作を提案する。

### 3.5 UI 文字列は必ず翻訳関数を通す
C++ は `tr()`、QML は `qsTr()`。生の文字列リテラルを UI に直接渡すことをコーディング規約で禁止する。
詳細は [10章](design/10-i18n.md)。

### 3.6 プラットフォーム依存コードの隔離
`#ifdef` をロジックの中に散らさない。プラットフォーム依存の実装は
`src/platform/win/` `src/platform/mac/` に分離し、CMake でビルド対象を切り替える。
インタフェースは共通ヘッダに置く。

> **例外**: 数行で完結する自明な分岐(`IPlugView` の platform type 文字列選択など)は
> インライン `#if defined(Q_OS_WIN)` を許容する。

## 4. 用語集

| 用語 | 意味 |
|---|---|
| **トラック / レイヤー** | 本書では同義。`Track` クラスのインスタンス。上に置かれたトラックが手前に合成される |
| **クリップ** | トラック上に配置された、開始・終了を持つ編集単位 (`Clip` 派生) |
| **字幕区間** | 字幕トラック上の 1 クリップ (`SubtitleClip`)。SRT の 1 キューに対応 |
| **タイムベース** | 1 フレームの長さを表す有理数。`60000/1001` なら 1 フレーム = 1001/60000 秒 |
| **PDC** | Plugin Delay Compensation。プラグインが持つ処理遅延を他トラックの遅延で相殺すること |
| **In点 / Out点** | 区間の開始フレーム / 終了フレーム(Out は排他的、すなわち `[in, out)`) |
| **コミット** | AI 生成などの非同期結果を、実際にタイムラインへ反映すること |
| **プロバイダ** | AI 生成の実行主体 (ローカル ONNX / リモート API / sidecar プロセス) |
| **グリフラン** | 字幕テキストをレイアウト後、文字単位に分解した描画単位 |
| **AIトラック / 絵コンテトラック** | `TrackType::Storyboard` のトラック。メディアではなく演出指示が載る。合成には参加しない |
| **カット** | AIトラック上の 1 区間 (`CutClip`)。絵コンテの 1 コマにあたる演出指示の単位 |
| **出力バインディング** | 1 カットが生む 1 成果物の仕様 (`OutputBinding`)。役割・配置先・パラメータ・生成状態を持つ |
| **Story Bible** | プロジェクト単位の作品設定 (キャラクター / ロケーション / 画風)。全カットのプロンプトへカスケードする |
| **アニマティック** | 未生成カットをボード画像 + TTS プレビューで仮再生すること |
| **パネル** | ドックエリアへ配置できる UI の単位。タブ化できるのは [1.7.1](design/01-architecture.md) の 8 種のみ |
| **ライブラリ** | 素材や効果をフォルダで整理して並べるパネル。プロジェクト所有の `mediaLibrary` と、アプリ所有の `effectLibrary` の 2 枚がある ([1.7.5](design/01-architecture.md)) |
| **フィルター** | クリップ 1 個に掛ける映像処理 (`VideoFilterInstance`)。配列順に適用され、その後で合成される ([3.9](design/03-timeline-render.md)) |
| **トランジション** | クリップ境界に置く映像の切り替え (`Transition`)。クリップ同士は重ねず、境界に付く別オブジェクトとして持つ ([3.10](design/03-timeline-render.md)) |
| **タイトル** | 映像トラックに置けるテロップ (`TitleClip`)。描画は字幕と同じ経路だが、書き出し時に字幕トラックとして分離されない ([3.11](design/03-timeline-render.md)) |
| **ドックエリア** | パネルを収める領域 (左 / 右 / 下 / 中央)。同一エリアに複数入るとタブグループになる |
| **承認状態** | カットの進行状態 (未着手 / ラフ / 確認中 / 承認済み)。承認済みのみが本生成へ進む |

## 5. 非機能要件

| 項目 | 目標値 |
|---|---|
| プレビュー | 3840x2160 / 60fps / 4 レイヤー同時合成でフレーム落ちなし |
| プレビュー遅延 | シーク操作からの表示更新まで 100ms 以内 |
| オーディオ遅延 | 出力バッファ 256 サンプル @48kHz (約 5.3ms) で動作可能 |
| プロジェクト起動 | 1000 クリップのプロジェクトを 2 秒以内にロード |
| メモリ | 4K 編集時に 8GB 以内(フレームキャッシュ上限を含む) |
| AI 生成 | UI をブロックしない。生成中も編集・再生を継続できる |
|セキュリティ|ユーザプロファイル、クラウドサービスのユーザId/パスワードなどはプロジェクトごとの保存データではなくアプリケーション固有の永続ファイルに保存される。外部へのエクスポートされないとする|
|JSONファイルのフォーマット|文書化すること|
